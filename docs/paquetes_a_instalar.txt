pip install sphinx-rtd-theme    // Para instalar el tema de la documentación
pip install breathe  // Extensión para unir doxygen con sphinx
pip install sphinx-intl   // Extensión para tener una pagina multilengua

pip install pyyaml  // Para poder leer el YAML dentro de conf.py