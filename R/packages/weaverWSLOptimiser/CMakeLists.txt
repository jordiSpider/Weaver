
if(RELEASE_MODE)
    set(WEAVER_EXECUTABLE "${EXECUTABLE_OUTPUT_PATH}/optimiser${CMAKE_EXECUTABLE_SUFFIX_CXX}")
else()
    set(WEAVER_EXECUTABLE "${EXECUTABLE_OUTPUT_PATH}/optimiser${CMAKE_DEBUG_POSTFIX}${CMAKE_EXECUTABLE_SUFFIX_CXX}")
endif()

set(WRAPPER_FILE_TEMPLATE "${CMAKE_CURRENT_SOURCE_DIR}/R/wrapper.r.in")
set(WRAPPER_FILE "${CMAKE_CURRENT_SOURCE_DIR}/R/wrapper.r")

configure_file(${WRAPPER_FILE_TEMPLATE} ${WRAPPER_FILE} @ONLY)
